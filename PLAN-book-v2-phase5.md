# book-v2 フェーズ5指示書 — hooks を strategy に1本追加

作業ブランチ: `rewrite/book-v2`（`main` は変更しない）
前提: フェーズ4完了後に着手する（旧リンク一掃と混線させない）
必読: `AGENTS.md` → `PLAN-book-v2.md` → `CLAUDE.md` → 本指示

## なぜ独立フェーズか

hooks は既存ページの修正ではなく、**新規の設計論点の追加**である。
フェーズ4（機械的なリンク・用語整合）と混ぜると、両方とも中途半端になる。
よってフェーズ4の完了後に実施する。

## 目的

AI ツールの動作の節目に処理を自動実行・制御する仕組み（hooks）を、
本書の層モデルの外にある **実行境界（ハーネス側）の機構** として位置づけ、
strategy に1ページ追加する。

やること:

1. 日本語本文 `docs/ja/strategy/hooks.md` を新規作成（常体・文書体）
2. 英語 `docs/strategy/hooks.md` を対称作成
3. サイドバー第IV部「戦略・展開」に項目を追加（日英）
4. 近接ページから短い相互リンクを1〜2本足す（本文の長い改稿はしない）

やらないこと:

- 五層を六層に拡張すること
- 特定製品（Claude Code 等）の hooks API リファレンス化
- フェーズ4範囲の再作業
- `main` へのマージ

## ページの位置づけ

| 項目         | 内容                                                             |
| ------------ | ---------------------------------------------------------------- |
| 所属         | 第IV部 周辺・展開（`strategy/`）                                 |
| パス         | `docs/ja/strategy/hooks.md` / `docs/strategy/hooks.md`           |
| 層モデル     | 層ではない。Harness / 実行境界の機構                             |
| 書名との関係 | 設計判断（いつ使うか、何と混ぜないか）を書く。操作手順は書かない |

## 本文に含める節（日本語を先に書く）

推奨構成:

1. 定義 — 動作の節目に、処理を自動実行・制御する仕組み
2. 五層との関係 — なぜ第六層にしないか（表で短く）
3. 何と違うか — Skills / MCP / Sub-agent 品質ゲート / Doctrine との対比表
4. いつ使うか — 宣言だけでは守らせきれない強制、監査、危険操作の阻止、定型の後処理
5. いつ使わないか — 知識の置き場にする、外部システムの本体接続を hooks だけで済ませる、等
6. 近接する既存ページへの案内
7. 要約

対比表の核:

| 仕組み               | 役割                                 |
| -------------------- | ------------------------------------ |
| Skills               | 変わらない知識・手順（読まれる）     |
| MCP                  | 外のシステムへの接続                 |
| Sub-agent 品質ゲート | 別コンテキストへの委任と検証         |
| hooks                | 同一実行のライフサイクルへの割り込み |
| Doctrine             | 目的・禁止・優先順位（物差し）       |

hooks は Doctrine / Skills の一部を **実行時に機械で強制する手段** になり得る、と書いてよい。
ただし hooks 自体が物差しではない、と明記する。

## 相互リンク（追記のみ）

次のいずれか2つ程度に、hooks への1行リンクを足す。長文改稿はしない。

- `docs/ja/strategy/harness-engineering-mapping.md`
- `docs/ja/strategy/loop-engineering.md`
- `docs/ja/strategy/permission-vs-authority.md`
- または `docs/ja/part-4/limits.md`（実行時強制の一文）

英語も対称。

## サイドバー

`ja.ts` / `en.ts` の第IV部「戦略・展開」内に追加する。
位置は `harness-engineering-mapping` の近くがよい。

- 日本語ラベル例: `Hooks（実行時フック）`
- 英語ラベル例: `Hooks`

## 文体

- 常体。口語・絵文字禁止
- 製品名は例示に留め、API 一覧にしない
- MUST / SHOULD を使う場合は日本語併記

## `PLAN-book-v2.md` の更新

| #   | 内容                     | 備考                                          |
| --- | ------------------------ | --------------------------------------------- |
| 12  | hooks を strategy に追加 | 詳細は `PLAN-book-v2-phase5.md`。層にはしない |

## 終了条件と報告

終了条件:

- 日英の hooks ページが存在する
- サイドバー日英に載っている
- 五層定義を変更していない
- 近接ページから辿れる

報告:

1. 追加・変更ファイル一覧
2. サイドバー上の位置
3. 相互リンクを足したページ
4. 残課題（製品別 how-to は本書の外、と明記）
