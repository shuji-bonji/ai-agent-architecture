# AGENTS.md

本書（本サイト）を編集するエージェントへの指示である。実装の詳細（i18n、Mermaid、Alerts、前後ナビ）は `CLAUDE.md` に従う。方針が衝突する場合は、本ファイルを優先する。

## 成果物

- リポジトリは変更しない。公開 URL も維持する。
- 日本語書名: **LLMエージェントの設計**
- 英語書名: **LLM Agent Design Architecture**
- 旧称 "AI Agent Architecture" および副題 "MCPの、その先へ" は入口に使わない。
- これは実行手順書ではない。LLM の構造的制約を前提とした **設計の文書** である。

## 改定の原則

1. 同一リポジトリで再編する。新リポジトリは作らない。
2. 既存の実践知（Skills / MCP / Doctrine / Memory / Agent / A2A）は捨てず、読む順を組み替える。
3. 物語の起点は MCP ではない。起点は **LLM（基盤モデル）の構造的制約** である。
4. スコープの説明は FAQ による釈明にしない。序章で定義する。
5. `main` を直接壊さない。作業は専用ブランチ（例: `rewrite/book-v2`）で行う。
6. 現行を消す前に、必要なら `v1` タグまたは `archive/v1/` を残す。
7. 旧パスは可能なかぎりリダイレクトする。被リンクを切らない。

## スコープ

扱うもの:

- 基盤モデル（主に LLM。VLA 等を含む）を推論の中核とするエージェントの設計
- 層: Doctrine / Agent / Skills / Memory / MCP
- 再利用、保守、引き渡しのための判断基準

扱わないもの:

- 強化学習エージェント、古典的規則ベース、制御理論そのもの
- LLM 内部構造の詳細（姉妹サイトへ委ねる。ただし設計に必要な制約は本書内で要約する）
- 特定製品の操作手順
- 情報ガバナンスの制度設計そのもの

## 読者

試作ではなく継続利用する前提でエージェントを組み立てる技術者。機械学習の研究経験は必須としない。動かす先にある設計・保守・拡張・引き渡しを対象とする。

## 姉妹資料

| 関心                   | 資料                                  |
| ---------------------- | ------------------------------------- |
| 理解する（制約の由来） | understanding-llm-through-claude-code |
| 設計する               | 本リポジトリ                          |
| 運用へ適用する         | 別資料（準備中）                      |

Why は姉妹サイト、What/How は本書、という分担は維持する。ただし本書の入口は、姉妹サイトを読まなくても成立する序章を持つ。

## 日本語文体（必須）

日本語本文は技術書の文書体とする。

- 本文は常体（である調）で統一する。ですます調と混ぜない。
- 1 文は 1 義。長い因果は文を分ける。
- 口語を本文に置かない。禁止例: 「腹落ち」「逆に言えば」「調べて来られた方へ」「〜の、その先へ」
- 絵文字、👈、💡 を見出しや本文に使わない。
- 主張を `≠` `⊋` などの記号だけで書かない。日本語の文として書き切る。
- 用語は初出で定義する。英語の層名（MCP, Skills, Doctrine, Memory, Agent）は固有名として残してよい。一般概念は日本語を優先する。
- MUST / SHOULD は RFC 2119 の意味で使う。本文では「しなければならない」「するのがよい」を併記し、記号だけに意味を預けない。
- FAQ 調の防御文（「〜ではないのか」から入る釈明）を序論にしない。定義から入る。

英語版は同等の内容を、英語の技術書として書く。口語のキャッチコピーを本文の論理にしない。英日は対称に更新する（`CLAUDE.md` の i18n 規則）。

## 執筆の優先言語

v2 の論理原稿は **日本語を先に確定** し、英語はそれに対応させる。現行 `CLAUDE.md` の「英語がメイン」は、v2 完了まで本ファイルが上書きする。サイトの locale 構成（`docs/` = 英語、`docs/ja/` = 日本語）は維持する。

## 構成（目標）

- 序章: スコープ、読者、本書の問い
- 第I部 前提: 制約の要約（機序の詳細には入らない）
- 第II部 モデル: 層と配置基準
- 第III部 各層: Skills / MCP / Doctrine / Memory / Agent
- 第IV部 構成と展開: パターン、限界、物理世界、プロンプトの分解

既存の `docs/concepts/01` 以降は、番号を機械的に維持することより、上記の論理順を優先する。パス変更時はリダイレクトを付ける。

## 作業手順

エージェントは、ユーザーが指定した範囲だけを変更する。一括の全面置換は、明示されたとき以外行わない。

推奨順:

1. 本ファイルと序章（日本語）を置く
2. ホームページとサイトタイトルを新書名へ替える
3. 第I部（制約の要約）
4. 第II部（層モデル。現行 5 層と用語を一致させる）
5. 既存各論を第III・IV部へ再配置し、文書体へ直す
6. FAQ のスコープページを、序章への短い参照に縮小する
7. `CLAUDE.md`・サイドバー・README を新構成へ追随させる
8. 英語版を対称更新する

各ステップの終わりに、変更したページの目的と、残課題を短く報告する。

## 禁止

- リポジトリの新規作成、リポジトリ名の変更
- 公開 URL の破壊（リダイレクトなしのパス削除）
- 実践例（自作 MCP / Skills）の削除
- ハーネス製品の操作マニュアル化
- 思想の全面破棄と、空のテンプレートへの置き換え
- App Builder 用の別アプリとしてサイトを再実装すること
